# Part of DM 4.7
# Runs ALWAYS

{
    "topic_types": [
        {
            "value": "Disk Quota (MB)",
            "uri": "dm4.files.disk_quota",
            "dataTypeUri": "dm4.core.number"
        }
    ]
}
