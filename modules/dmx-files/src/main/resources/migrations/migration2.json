# Part of DMX 5.0
# Runs ALWAYS

{
    "topic_types": [
        {
            "value": "Disk Quota (MB)",
            "uri": "dmx.files.disk_quota",
            "dataTypeUri": "dmx.core.number"
        }
    ]
}
