# Part of DM 4.7
# Runs ALWAYS

{
    "topic_types": [
        {
            "value": "Disk Quota (MB)",
            "uri": "dmx.files.disk_quota",
            "dataTypeUri": "dmx.core.number"
        }
    ]
}
